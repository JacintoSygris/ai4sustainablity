# Security

Back to the [technical manual](../index.md).

Bridge route. See [secrets and parameters](../secrets-and-parameters.md), [proxy and TLS](../proxy-tls.md), [mail and OAuth](../mail-oauth.md) and [operations](../operations.md).

Minimum rules: no secrets in Git, no private ports exposed, secure cookies over HTTPS, `APP_DEBUG=false`, backups encrypted according to operator policy and documents disabled until extraction is implemented.
